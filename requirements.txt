# PySpark: pin to the latest 4.0.x available on PyPI. (4.1.1 is not published there yet;
# `pip` then fails with "No matching distribution found".)
pyspark==3.5.2

# HTTP client for data.py (Socrata). Let pip resolve compatible transitive versions.
requests>=2.28.0,<3

# HDFS pure-python client to upload without relying on local Hadoop install
hdfs==2.7.3

# XGBoost Spark estimator (SparkXGBRegressor; requiere pandas en driver/executors)
xgboost>=2.0.0,<3
pandas>=1.5.0,<3
scikit-learn>=1.0.0,<2
pyarrow>=17.0.0,<19
matplotlib>=3.5.0,<4

# Autoencoder (encoder_pipeline.py + ml_autoencoder.py)
torch>=2.0.0,<3          # PyTorch — autoencoder model + TorchDistributor DDP training
scipy>=1.9.0,<2          # Shapiro-Wilk test for adaptive anomaly threshold

# Dashboard
streamlit>=1.35.0,<2
plotly>=5.20.0,<6
