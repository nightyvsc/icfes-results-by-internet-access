# PySpark: pin to the latest 4.0.x available on PyPI. (4.1.1 is not published there yet;
# `pip` then fails with "No matching distribution found".)
pyspark==4.0.2

# HTTP client for data.py (Socrata). Let pip resolve compatible transitive versions.
requests>=2.28.0,<3
